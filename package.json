{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.17.3"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy-sender-sepolia": "npx hardhat run scripts/deploy_sender.ts --network sepolia",
    "deploy-receiver-mumbai": "npx hardhat run scripts/deploy_receiver.ts --network mumbai"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.7.1",
    "@chainlink/contracts-ccip": "^0.7.6",
    "dotenv": "^16.3.1"
  }
}
